@model IEnumerable<WebApplication8.Models.PermissionUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Permissions.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Permissions.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PermissionUserId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PermissionUserId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PermissionUserId })
        </td>
    </tr>
}

</table>
